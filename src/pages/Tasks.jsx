import React from 'react';

function Tasks() {
  return (
    <div>
      <h2>Tasks</h2>
      <p>will put content later, if ur a member it only shows u ur tasks in detail, the recently added and the recently deleted tasks..., if ur a team leader it will allow u to add and delete tasks and see all the tasks of each member of the project</p>
    </div>
  );
}

export default Tasks;