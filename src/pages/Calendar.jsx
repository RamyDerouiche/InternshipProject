import React from 'react';

function Calendar() {
  return (
    <div>
      <h2>Calendar</h2>
      <p>will put content later, if ur a member its calendar with all the tasks, u can navigate through it, if u are a team leader u see a calendar with all the tasks of each memeber (can add the image of each member) </p>
    </div>
  );
}

export default Calendar;