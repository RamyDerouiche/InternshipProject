import React from 'react';

function Dashboard() {
  return (
    <div>
      <h2>Dashboard</h2>
      <p>will put content later, if ur a member u can see ur tasks of today and the notes left by the project leader with them , the added tasks, recent activity of the project</p>
    </div>
  );
}

export default Dashboard;